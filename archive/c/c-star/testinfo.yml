folder:
  extension: ".cx"
  naming: "hyphen"

container:
  image: "rzuckerm/c-star"
  tag: "2023.05.17-dev"
  build: "cx -o {{ source.name }} {{ source.name }}{{ source.extension }}"
  cmd: "./{{ source.name }}"
