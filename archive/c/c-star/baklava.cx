void main()
{
    for (int n in -10..11) {
        int num_spaces = abs(n);
        int num_stars = 21 - 2 * num_spaces;
        println(" ".repeat(num_spaces) + string("*".repeat(num_stars)));
    }
}
