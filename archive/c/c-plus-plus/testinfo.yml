folder:
  extension: ".cpp"
  naming: "hyphen"

container:
  image: "gcc"
  tag: "15.2"
  build: "g++ -std=c++20 -o {{ source.name }} {{ source.name }}{{ source.extension }} -pthread -lm"
  cmd: "./{{ source.name }}"
