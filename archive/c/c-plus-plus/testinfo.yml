folder:
  extension: ".cpp"
  naming: "hyphen"

container:
  image: "gcc"
  tag: "15.2"
  build: "g++ -std=c++23 -pthread -o {{ source.name }} {{ source.name }}{{ source.extension }}"
  cmd: "./{{ source.name }}"
