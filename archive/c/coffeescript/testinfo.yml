language_display_name: "CoffeeScript"

folder:
  extension: ".coffee"
  naming: "hyphen"

container:
  image: "codesignal/coffeescript"
  tag: "v7.1"
  cmd: "coffee {{ source.name }}{{ source.extension }}"
