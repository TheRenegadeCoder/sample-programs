language_display_name: "CoffeeScript"

folder:
  extension: ".coffee"
  naming: "hyphen"

container:
  image: "cyberdojofoundation/coffeescript"
  tag: "595c28c"
  cmd: "coffee {{ source.name }}{{ source.extension }}"
