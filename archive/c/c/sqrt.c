#include<stdio.h>
#include<math.h>
//program used To find Square Root of Any Number

void main()

            {
                float a; 
                scanf(" %f ",&a);
                printf("Square Root Of numbers: %f",pow(a,1/2.0)); 
		// pow is function in math.h takes parameters as pow(number,exponent);

            }
