folder:
  extension: ".c"
  naming: "hyphen"

container:
  image: "gcc"
  tag: "8.3" 
  build: "gcc -pthread -o {{ source.name }} {{ source.name }}{{ source.extension }} -lm"
  cmd: "./{{ source.name }}"
