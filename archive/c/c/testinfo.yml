folder:
  extension: ".c"
  naming: "hyphen"

container:
  image: "gcc"
  tag: "15.2" 
  build: "gcc -std=c2x -o {{ source.name }} {{ source.name }}{{ source.extension }} -pthread -lm"
  cmd: "./{{ source.name }}"
