folder:
  extension: ".c"
  naming: "hyphen"

container:
  image: "gcc"
  tag: "15.2" 
  build: "gcc -std=c2x -pthread -lm -o {{ source.name }} {{ source.name }}{{ source.extension }}"
  cmd: "./{{ source.name }}"
