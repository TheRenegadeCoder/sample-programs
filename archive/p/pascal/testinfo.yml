folder:
  extension: ".pas"
  naming: "underscore"

container:
  image: "freepascal/fpc"
  tag: "3.2.2-alpine-3.19-minimal⁠"
  build: "fpc {{ source.name }}{{ source.extension }}"
  cmd: "./{{ source.name }}"
