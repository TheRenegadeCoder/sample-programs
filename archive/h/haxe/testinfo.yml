folder:
  extension: ".hx"
  naming: "pascal"

container:
  image: "haxe"
  tag: "4.2-buster"
  build: "haxe --main {{ source.name }} --python {{ source.name }}.py"
  cmd: "python {{ source.name }}.py"
