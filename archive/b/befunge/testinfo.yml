folder:
  extension: ".be"
  naming: "hyphen"

container:
  image: "baekjoon/onlinejudge-tbc"
  tag: "1.0"
  build: "/tbc {{ source.name }}{{ source.extension }} > Main.c && /usr/bin/gcc -o Main Main.c"
  cmd: "./Main"
