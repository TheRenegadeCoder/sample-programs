folder:
  extension: ".erl"
  naming: "underscore"

container:
  image: "erlang"
  tag: "22.1.5-alpine"
  build: "erlc {{ source.name }}{{ source.extension }}"
  cmd: "erl -noshell -s {{ source.name }} start -s init stop"
