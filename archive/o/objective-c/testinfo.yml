folder:
  extension: ".m"
  naming: "hyphen"

container:
  image: "codesignal/objective-c"
  tag: "v9.4.0"
  build: "gcc -o {{ source.name }} -l objc {{ source.name }}{{ source.extension }}"
  cmd: "./{{ source.name }}"
