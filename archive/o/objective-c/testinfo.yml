language_display_name: "Objective-C"

folder:
  extension: ".m"
  naming: "hyphen"

container:
  image: "pascalecu/clang-objc2-gnustep"
  tag: "clang22-ubuntu26.04"
  build: "objc -o {{ source.name }} {{ source.name }}{{ source.extension }}"
  cmd: "./{{ source.name }}"
