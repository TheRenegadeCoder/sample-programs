folder:
  extension: ".m"
  naming: "hyphen"

container:
  image: "codesignal/objective-c"
  tag: "v9.4.0"
  build: "clang -o {{ source.name }} {{ source.name }}{{ source.extension }}"
  cmd: "./{{ source.name }}"
