folder:
  extension: ".ml"
  naming: "hyphen"

container:
  image: "ocaml/ocaml"
  tag: "latest"
  build: "ocaml -o {{ source.name }} {{ source.name }}{{ source.extension }}"
  cmd: "./{{ source.name }}"
