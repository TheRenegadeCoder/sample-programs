folder:
  extension: ".odin"
  naming: "hyphen"

container:
  image: "gonz/odin"
  tag: "dev-2024-06"
  build: "odin build {{ source.name }}{{ source.extension }} -file -out:{{ source.name }}"
  cmd: "./{{ source.name }}"
