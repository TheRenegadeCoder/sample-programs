folder:
  extension: ".odin"
  naming: "hyphen"

container:
  image: "yeongjukang/odin"
  tag: "alpine-dev-2022-08"
  build: "odin build {{ source.name }}{{ source.extension }} -file"
  cmd: "./{{ source.name }}.exe"
